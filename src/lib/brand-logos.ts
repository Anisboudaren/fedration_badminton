import logoDarkText from "@/assets/main logo.png";
import type { AssetImport } from "@/lib/utils";

/** Black/dark text — for light backgrounds (sticky header, cards, admin). */
export const LOGO_DARK_TEXT: AssetImport = logoDarkText;

/** White text — for dark backgrounds (hero, overlay nav, footer). */
export const LOGO_WHITE_TEXT = "/logo/logo%20%20white%20text%20.png";
